{
  "name": "clubsync",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "db:push": "cd backend && alembic upgrade head",
    "db:generate": "cd backend && alembic revision --autogenerate -m"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.1.0"
}
